# GeoAnnotator Base Requirements
# Core dependencies shared across all environments

# Django and GeoDjango
Django>=4.2,<4.3
psycopg2-binary>=2.9  # PostgreSQL adapter
django-environ>=0.11  # Environment variable management
dj-database-url>=2.0  # Database URL parser
django-grappelli>=2.19  # Admin interface enhancement

# GeoDjango dependencies
# IMPORTANT: GDAL version must match system libgdal version
# Debian bookworm (Python 3.11-slim) ships with GDAL 3.6.x
GDAL>=3.6,<3.7
Pillow>=10.0  # Image processing

# Django REST Framework
djangorestframework>=3.14,<3.15
djangorestframework-simplejwt>=5.3  # JWT authentication
djangorestframework-gis>=1.0  # GIS support for DRF
django-cors-headers>=4.3  # CORS support
django-filter>=23.5  # Filtering support

# File storage
django-storages>=1.14  # S3-compatible storage support
boto3>=1.34  # AWS SDK for S3

# Export/Import libraries
geopandas>=0.14  # GeoJSON, shapefile support
gpxpy>=1.6  # GPX format
simplekml>=1.3  # KML format

# Email
bleach>=6.1  # HTML sanitization for rich text
mailjet-rest>=1.3  # Mailjet API client for HTTP email sending

# Security
django-fernet-fields-v2>=0.9  # Email encryption (compatible with Django 4.2+)
django-ratelimit>=4.1  # Rate limiting for API endpoints

# Background Tasks
celery>=5.3  # Distributed task queue
redis>=5.0  # Message broker for Celery

# Utilities
humanize>=4.10  # Human-readable numbers
python-dateutil>=2.8
pytz>=2024.1
requests>=2.31  # HTTP library for downloading external icons
